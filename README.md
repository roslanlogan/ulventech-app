# Ulventech-app

## Getting start

1. Install dependencies
    `npm install`

2. Setup environment variable 
    > create `.env.local` file and add following details

    ```
    API_URL=
    ```
    > export config env

3. Start server
        npm run dev

## Prod build
1. Build assets

		npm run build

